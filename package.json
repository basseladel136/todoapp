{
  "name": "todo-application",
  "version": "1.0.0",
  "description": "Modernized full-stack ToDo App — React 19 + Vite frontend, Express + Supabase backend",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build": "npm run build:server && npm run build:client",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "lint": "npm --prefix server run lint && npm --prefix client run lint"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2"
  }
}
